package logic.data;

/**
 * Enum als Aufzählung der möglichen Positionen, welche von getAvailablePosition(...) verarbeitet
 * werden können, wird für die Hasenkarten benötigt.
 *
 * @author github.com/batscs
 */
public enum GamePosition {
    DEFAULT,
    NEXTCARROT,
    PREVIOUSCARROT,
    FORWARDS,
    BACKWARDS,
}
